---
apiVersion: v1
kind: Service
metadata:
  name: mysql-volsync
  labels:
    app: mysql-volsync
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: mysql-volsync
  type: ClusterIP
